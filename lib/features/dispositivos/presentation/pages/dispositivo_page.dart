import 'package:flutter/material.dart';

class DispositivoPage extends StatelessWidget {
  const DispositivoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Dispositivos"),
      ),
    );
  }
}