import 'package:flutter/material.dart';

class PersonasPages extends StatelessWidget {
  const PersonasPages({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Personas"),
      ),
    );
  }
}