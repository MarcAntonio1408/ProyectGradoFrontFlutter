import 'package:flutter/material.dart';

class DeteccionesPages extends StatelessWidget {
  const DeteccionesPages({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Detecciones"),
      ),
    );
  }
}